package com.example.mvvmbyshahrukh.repository

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import java.lang.IllegalArgumentException

class VastraViewModelFactory /*: ViewModelProvider.Factory*/ {

/*
    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(LoginViewModel::class.java)) {
            return LoginViewModel(
                loginRepository = VatraRepository()) as T
        }
       else  if (modelClass.isAssignableFrom(OrderStatusViewModel::class.java)) {
            return OrderStatusViewModel(VatraRepository()) as T
        }
        throw IllegalArgumentException("UnKnown View Model class")
    }
*/
}